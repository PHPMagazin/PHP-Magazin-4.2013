/* * Beispiel JavaScript zur Demonstration von asynchronen Unit Tests mit QUnit. */var persistence = new Persistence();//stoppt den Test Runner von QUnitasyncTest("Laden von JSON Objekten", 2, function() {  //starte die asynchrone Funktion und gibt die Tests in einer Callback-Funktion mit.  persistence.load("objects.json", function() {    var expectedListNames = ["Humbug", "Einkaufsliste", "Test", "Cool"];    var listNames = persistence.getTodoListNames();    equal(listNames.length, expectedListNames.length, "Es müssen genau so viele Listen sein, wie erwartet");    deepEqual(listNames, expectedListNames, "Die ListNames müssen denen der Datei entsprechen.");    // Startet den QUnit-internen Test Runner    start();  });});